import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/providers/theme-provider";
import { ErrorProvider } from "@/components/providers/error-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Vitordo - Intelligent Task Management",
  description: "Transform your natural language input into organized, scheduled tasks with AI-powered assistance.",
  keywords: ["task management", "AI", "productivity", "scheduling", "timeline"],
  authors: [{ name: "Vitordo Team" }],
  viewport: "width=device-width, initial-scale=1",
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#ffffff" },
    { media: "(prefers-color-scheme: dark)", color: "#0a0a0a" },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen bg-background text-foreground`}
      >
        <ThemeProvider defaultTheme="light" storageKey="vitordo-theme">
          <ErrorProvider maxErrors={10} defaultRetries={3}>
            {children}
          </ErrorProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
